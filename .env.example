# DIB Kuran API Yapılandırması
# Bu dosyayı .env.local dosyasına kopyalayın ve gerçek değerlerinizi girin
# Bu değerler için: INSTALLATION.md dosyasına bakın
DIB_KURAN_API_BASE_URL=https://t061.diyanet.gov.tr/apigateway/kuran
DIB_KURAN_API_TOKEN=your_api_token_here

# Temel uygulama ayarları (Opsiyonel - henüz altyapı hazır değil)
# FRONTEND_APP_ENV=development
# FRONTEND_APP_DEBUG=true
# FRONTEND_APP_PORT=3000
# FRONTEND_APP_URL="http://localhost:${FRONTEND_APP_PORT}"
# FRONTEND_APP_NAME="Kuran-ı Kerim Dijital Diyanet Projesi"

# Analytics Ayarları (Opsiyonel - ileride eklenebilir)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX

# Database Ayarları (ileride kullanıcı hesapları için)
# DATABASE_URL=postgresql://username:password@localhost:5432/kuran_db
# DATABASE_POOL_SIZE=10

# Redis Cache Ayarları (ileride performans için)  
# REDIS_URL=redis://localhost:6379
# REDIS_TTL=3600

# Email Ayarları (ileride bildirimler için)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# S3/CDN Ayarları (ileride ses dosyaları için)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_REGION=eu-west-1
# AWS_S3_BUCKET=kuran-audio-files

# JWT Ayarları (ileride authentication için)
# JWT_SECRET=your_super_secret_jwt_key_here
# JWT_EXPIRES_IN=7d

# Rate Limiting Ayarları (ileride API koruması için)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100